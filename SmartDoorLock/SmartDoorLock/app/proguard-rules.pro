# Add project specific ProGuard rules here.
# Minification is disabled by default for this exhibition build (see app/build.gradle.kts),
# so this file is not active. If you turn isMinifyEnabled on later, verify Room and
# TextToSpeech still work correctly, since both rely on reflection/dynamic lookups.
